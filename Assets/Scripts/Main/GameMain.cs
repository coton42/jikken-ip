using UnityEngine;

namespace Jikken.GameMain
{
	public class GameMain : MonoBehaviour
	{
		// View（MonoBehaviour）を追加した場合はここに記述する
		// 更に、シーン側でMainオブジェクトに当該Viewをアタッチする
		// 例:
		// [SerializeField] BallView ballView;

		void Awake() { }

		void Start() { }
	}
}
